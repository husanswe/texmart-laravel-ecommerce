/**
 * Company contact details, defined once. Every phone number in the UI comes
 * from here so there is never a second copy to fall out of sync.
 */

/** Dial string for a `tel:` href — no spaces. */
export const PHONE_TEL = 'tel:+998998856458'

/** Human-readable number for display: grouped with spaces, no dashes. */
export const PHONE_DISPLAY = '+998 99 885 64 58'

export const EMAIL = 'info@texmart.uz'
